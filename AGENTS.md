# file-watch

Production-quality file watcher CLI. Watches directories for file changes using polling and triggers commands.

## For AI agents
- scripts/watch.py is the main entrypoint with full CLI
- Use --json for machine-readable output
- Use --once for single-scan mode
- Use --dry-run to test without executing commands
